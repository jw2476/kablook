{
  "name": "svelte-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rollup -c",
    "dev": "ln -sf ../node_modules . && rollup -c -w",
    "validate": "svelte-check"
  }
}
