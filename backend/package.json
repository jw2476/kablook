{
  "main": "dist/index.js",
  "scripts": {
    "dev": "ln -s ../node_modules . && nodemon -e 'ts' -x 'npx tsc && node .'",
    "build": "npx tsc",
    "start": "ln -s ../node_modules . && node ."
  }
}
