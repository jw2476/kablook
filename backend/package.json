{
  "main": "./dist/index.js",
  "scripts": {
    "dev": "ln -sf ../node_modules . && nodemon -e 'ts' -x 'npx tsc && node .'",
    "build": "npx tsc",
    "start": "ln -sf ../node_modules . && node ."
  }
}
